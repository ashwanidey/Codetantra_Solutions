## Mathematical Functions
Q1. 
```
import math
num = float(input("num: "))
if num - int(num) >=0.5:
	print ("result:",math.ceil(num))
else:
	print("result:",math.trunc(num))
```
## Random Number Functions
Q1. 1
